# プロジェクト開発および保守ガイドライン (SAFETY.md)

このファイルは、AIアシスタント（Antigravity, Cursor, Claude等）および開発者が、本プロジェクトのコードを編集する際に遵守すべきルールをまとめたものです。**プロジェクトの破壊を防ぐため、以下の指示を厳守してください。**

---

## 1. 基本原則
- **既存コードの尊重**: 動作している既存のロジックを、十分な理由なく削除または大幅に変更しないでください。
- **インクリメンタルな変更**: 一度に大量のファイルを変更せず、小さくテスト可能な単位で変更を適用してください。
- **コメントの維持**: 既存のドキュメント、TODO、コメント、JSDocを勝手に削除しないでください。

## 2. スタイリングとUI
- **Fluent UI v9 の遵守**: 現在、プロジェクトは Fluent UI v9 (`@fluentui/react-components`) への移行を進めています。新しいコンポーネントはこれに従い、`makeStyles` / `useStyles` を使用してください。
- **レスポンシブデザイン**: モバイル（360px〜430px）を最優先（Mobile First）とし、PC/タブレットでもレイアウトが崩れないことを確認してください。
- **ダークモード対応**: `FluentProvider` によるテーマ管理を尊重し、ハードコードされた色（例: `color: "white"`）の使用を避け、トークンを使用してください。

## 3. 技術スタックとパターン
- **SettingsProvider**: ユーザー設定（外観、再生設定、APIベースURL等）は `src/contexts/SettingsContext.tsx` で一括管理されています。独自の `localStorage` 操作を増やさず、既存の Provider を介してください。
- **API 通信**: Invidious API との通信は統一されたパターンで行ってください。CORSやデコードのロジックが複雑なため、既存の `fetch` パターンを模倣してください。
- **Shaka Player**: 動画再生には Shaka Player を使用しています。プレイヤー周辺のロジックを変更する際は、再生の安定性を最優先してください。

## 4. 破壊を防ぐためのチェックリスト
- [ ] 変更後、モバイルビューでレイアウトが崩れていないか？
- [ ] ダークモード/ライトモードの両方で視認性が保たれているか？
- [ ] 既存の `useEffect` やライフサイクルを不用意に壊していないか？
- [ ] 新しい依存関係を無闇に追加していないか？

---
**注意**: このガイドラインに反する変更を提案または実行する場合、必ずその理由とリスクを説明してください。
